<Window x:Class="Books.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Books"
        xmlns:models="clr-namespace:Books.Models"
        xmlns:views="clr-namespace:Books.Views"
        xmlns:viewModels="clr-namespace:Books.ViewModels"
        xmlns:converters="clr-namespace:Books.Converters"
        mc:Ignorable="d"
        Title="Rodions Card-File" 
        WindowStartupLocation="CenterScreen"
        Height="500" Width="750">

    <Window.Resources>
        <converters:BookConverter x:Key="bookConverter" />
        <converters:AuthorConverter x:Key="authorConverter" />
        <converters:HouseConverter x:Key="houseConverter" />
        
        
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <StackPanel x:Name="LeftStackPanel"
                    Grid.Column="0"
                    Grid.Row="0">
            <TabControl>
                <TabItem Header="Books">
                    <ListView x:Name="booksGrid" ItemsSource="{Binding}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Title" Width="Auto" DisplayMemberBinding="{Binding Name}" />
                                <GridViewColumn Header="Author(s)" Width="Auto" DisplayMemberBinding="{Binding Authors}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </TabItem>
                <TabItem Header="Authors">
                    <ListView x:Name="authorsGrid" ItemsSource="{Binding}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" Width="Auto" DisplayMemberBinding="{Binding Name}" />
                                <GridViewColumn Header="Day of Birdth" Width="Auto" DisplayMemberBinding="{Binding DayOfBirdth}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </TabItem>
                <TabItem VerticalContentAlignment="Stretch"  Header="Houses">
                    <ListView x:Name="housesGrid" ItemsSource="{Binding}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" Width="Auto" DisplayMemberBinding="{Binding Name}" />
                                <GridViewColumn Header="City" Width="Auto" DisplayMemberBinding="{Binding City}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </TabItem>
            </TabControl>
        </StackPanel>
        <Grid x:Name="bookGrid"
              Grid.Column="1"
              DataContext="{Binding SelectedValue, ElementName=booksGrid}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Label Grid.Row="0" Grid.Column="0" Content="Title:"/>
            <Label Grid.Row="1" Grid.Column="0" Content="Author(s):"/>
            <Label Grid.Row="2" Grid.Column="0" Content="ISBN:"/>
            <Label Grid.Row="3" Grid.Column="0" Content="Pages:"/>
            <Label Grid.Row="4" Grid.Column="0" Content="Tags:"/>
            <Label Grid.Row="5" Grid.Column="0" Content="Publication Year:"/>
            <Label Grid.Row="6" Grid.Column="0" Content="Publication House:"/>
            <TextBlock Grid.Column="1" Grid.Row="0" Margin="3" Text="{Binding Name}"/>
            <TextBlock Grid.Column="1" Grid.Row="1" Margin="3" Text="{Binding Authors}"/>
            <TextBlock Grid.Column="1" Grid.Row="2" Margin="3" Text="{Binding ISBN}"/>
            <TextBlock Grid.Column="1" Grid.Row="3" Margin="3" Text="{Binding Pages}"/>
            <TextBlock Grid.Column="1" Grid.Row="4" Margin="3" Text="{Binding Tags}"/>
            <TextBlock Grid.Column="1" Grid.Row="5" Margin="3" Text="{Binding PublicationYear}"/>
            <TextBlock Grid.Column="1" Grid.Row="6" Margin="3" Text="{Binding House}"/>
        </Grid>
    </Grid>
</Window>
